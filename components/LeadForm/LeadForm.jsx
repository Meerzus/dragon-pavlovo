'use client'

import React from 'react';

function LeadForm({isForm, setIsForm}) {
    return (
        <div id={'leadForm'}>
            <div className="lead-background"></div>
            <form className="lead-form">

            </form>
        </div>
    );
}

export default LeadForm;